<{{#if url}}a href="{{url}}"{{/if}}{{#unless url}}button{{/unless}} {{#if type}}type="{{type}}"{{/if}} class="button {{#if modifier }}{{ modifier }}{{/if}}{{#unless modifier }}button--light{{/unless}}">
    {{text}}
</{{#if url}}a{{/if}}{{#unless url}}button{{/unless}}>